import React from "react";

// context

// component
import {
  FavoritesScreenView,
  FavoritesScreenText,
} from "./FavoritesScreen.styles";

const FavoritesScreen = () => {
  return (
    <FavoritesScreenView>
      <FavoritesScreenText>Favorites Screen</FavoritesScreenText>
    </FavoritesScreenView>
  );
};

export default FavoritesScreen;
